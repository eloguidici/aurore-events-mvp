/**
 * Token for Metrics Collector Service injection
 * Used for dependency injection to decouple services from concrete implementations
 */
export const METRICS_COLLECTOR_SERVICE_TOKEN = Symbol(
  'METRICS_COLLECTOR_SERVICE_TOKEN',
);

